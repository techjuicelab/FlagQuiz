# 확정된 결정

2026-09-15, 역인터뷰([INTERVIEW.md](INTERVIEW.md))의 차단 질문에 대한 아빠의 답이다.
이 문서가 [README.md](README.md)의 "권장 설계"보다 우선한다.

| # | 결정 | 권장안과 같은가 |
| --- | --- | --- |
| D1 | 매칭 축은 **나라 ↔ 그 나라 대표 명소**. 수도 제약 없음 | 같음 |
| D2 | 첫 배포는 **도감 카드에 그림 붙이기**, 2주 뒤 퀴즈 모드 1개 | 같음 |
| D3 | 세계지도는 **채점하는 퀴즈 모드**로 만든다 | **다름** (권장은 보상 연출) |
| D4 | 그림은 **194개국 전부 완성 후 한 번에** 공개 | **다름** (권장은 대륙 단위) |
| D5 | 상징물은 **나라당 1장 고정 + 예비 2개** 보유 | 같음 |
| D6 | 기록을 **축별로 분리**, 194칸 스티커는 유지하고 칸 안에 도장을 겹친다 | 같음 |
| D7 | 화풍은 **B · 그림책 과슈**로 확정 (2026-09-15 사용자 선택) | 확정 |
| D8 | **명소 축의 1:1은 완화** — 같은 범주도 그리되 구별 지침과 혼동군 배제로 가른다 | 같음 |
| D9 | 상징물의 **규칙2 양보 5건을 그대로 둔다** — 1:1이 우선 | 같음 |
| D10 | **종교 건축 7건을 외관만 그려 남긴다** (바티칸은 상징물만 교체) | 같음 |
| D11 | **아프리카 명소를 한 번 더 발굴한다** — 기준을 유명세에서 구별 가능성으로 | 같음 |
| D12 | **모자 6중만 재배치**, 야자 열매와 소금은 색으로 갈려 유지 | 같음 |
| D13 | **아프리카 동물 한도를 12개국(22%)으로** 올린다 | 같음 |
| D14 | **시리아 크락 데 슈발리에를 뺀다** — 팔미라와 기준을 맞춘다 | 같음 |

---

## D1 — 나라 ↔ 대표 명소

"수도의 랜드마크"가 아니라 "그 나라의 대표 명소"로 채점한다. 수도 이름은 기존 수도 모드가 계속 담당한다.

**이 결정으로 해소되는 것**

- 45개국의 어긋남이 사라진다. 자유의 여신상(뉴욕)·오페라하우스(시드니)·타지마할(아그라)·마추픽추(쿠스코)·예수상(리우)이 전부 유효해진다.
- `data/countries.js`의 `fact` 문장 최소 16개가 그대로 정답 해설이 된다 → **새 수아 음원 0개**.
  `scripts/build-site.mjs:13`의 배포 게이트를 건드리지 않는다.
- 커버리지 등급표를 다시 세어야 한다. 기존 C등급 146개국은 "수도 안에"라는 기준이었다.

**따라올 작업**

- 명소 카드에 "○○의 수도는 △△예요" 한 줄 병기. 이 문장은 194개국 전부 이미 녹음돼 있다.

## D2 — 도감 카드 먼저

`js/ui.js:64`(fact-box)와 `:65`(hint-box) 사이에 그림 블록 하나를 끼운다.
엔진·채점·테스트 변경 **0줄**. 스티커판(`screens.js:100`)에서 바로 보인다.

2주 뒤 퀴즈 모드를 **1개만** 얹는다. 만 4세에게 "상징물 모드"와 "명소 모드"의 차이는 설명이 불가능하다.
둘 다 그냥 "그 나라 그림"이다.

**아직 안 정한 것** — 모드의 손가락 조작 방식(끌어다 놓기 / 탭-탭 / 기존 사지선다).
2주 뒤 모드 작업 때 정한다. 현재 권장은 **기존 사지선다 재사용**이다:
그림 1장을 보여주고 국기 4장 중 고르게 하면 `reverse` 모드 구조(`app.js:600-607`)를 거의 그대로 쓸 수 있고,
민규가 이미 100% 아는 것(국기)으로 답하게 되어 "몰라서 못 맞힌 것"과 "조작을 못해서 못 맞힌 것"이 섞이지 않는다.

## D3 — 채점하는 지도 퀴즈 (권장안과 다름)

연출이 아니라 **채점 대상**으로 만든다. 권장안(보상 연출)보다 비싼 경로이므로 따라오는 작업을 명시한다.

**반드시 필요한 것**

1. **194개국 좌표** — Natural Earth 50m의 `LABEL_X`/`LABEL_Y`(사람이 손으로 보정한 라벨용 좌표). 약 6KB.
   소수 2자리로 저장한다. 특정 지도에 맞춘 사전 계산 x/y는 넣지 않는다 — 지도를 바꾸면 194개 값이 한꺼번에 무효가 된다.
2. **세계지도 도형** — Natural Earth 50m + Douglas-Peucker. 초기 실측은 tol 0.7·162KB(gzip 59KB)였고, HANDOFF의 허용 대안 0.5°를 육안 확인 후 채택했다. 2026-09-15 수정본은 92,742바이트, 194개국과 배제 지역 48개를 보존한다([MAP-REPAIR.md](MAP-REPAIR.md)).
   빌드 시점에 변환하고 결과 path 문자열만 커밋한다. 원본은 `.gitignore`. 퍼블릭 도메인이라 귀속 의무는 없지만 출처는 적는다.
3. **국가 코드 조인** — Natural Earth의 `ISO_A2`가 노르웨이·프랑스 등에서 `-99`다.
   `ISO_A2_EH` → `ISO_A2` → `WB_A2` 폴백으로 194개국 전부 매칭된다.
   `tests/run.mjs:114-124`의 국기 검사와 같은 방식으로 "빠진 것도 남는 것도 없음"을 단언한다.
4. **탭 대상은 도형이 아니라 고정 크기 핀** — 화면 폭 1040px에서 싱가포르 1.1px, 모나코 0.07px, 바티칸 0.016px다.
   `design/SPEC.md:37`이 터치 타깃 최소 44px을 요구하므로 도형을 탭 대상으로 삼으면 바티칸은 영원히 탭할 수 없다.
5. **국경선을 그리지 않는다** — 육지를 하나로 합친 실루엣. Natural Earth 50m에는 우리 194개국 외에 48개 도형이 더 있고(242 − 194 = 48. 설계 당시 46으로 적었던 것은 산술 오류다)
   그 목록이 `README.md:173-176`이 의도적으로 제외한 대상(대만·팔레스타인·코소보·서사하라·소말릴란드·북키프로스·그린란드·홍콩·남극)과 정확히 겹친다.
   그냥 빼면 모로코 남쪽과 이스라엘 옆에 흰 구멍이 남아 오히려 더 눈에 띈다.
   README에 "지도에서는 땅만 그리고 국경선은 나라를 가르는 선으로 쓰지 않는다"를 명문화한다.
6. **투영법은 등적장방형 평면** — `x=(lng+180)/360`, `y=(90-lat)/180` 두 줄. 가로:세로 정확히 2:1이라
   `.app` max-width 1040px 안에서 1040×520으로 떨어진다. 지구본 회전은 매 프레임 13,574개 좌표를 재투영해야 해 SVG로는 불가능하다.
7. **애니메이션은 CSS transition** — `ui.setMain()`이 `#main.innerHTML`을 통째로 갈아끼우므로 정리 코드가 0줄이다.
   rAF를 쓰면 `app.js:62-73`에 정리를 추가하고 그것을 호출하는 9개 지점(108·405·438·563·1135·1199·1400·1405·1413)을 전부 확인해야 하고,
   하나라도 빠지면 홈으로 나간 뒤에도 도는 유령 루프가 남는다.
8. **움직임 줄이기 대응** — `css/style.css:531-533`이 애니메이션과 전환을 **둘 다** 죽인다.
   `@keyframes`로 만들면 그 설정에서 핀이 출발 위치에 멈춰 **정답이 화면에 나타나지 않는다.**
   최종 상태를 먼저 지정하고 전환으로 잇는 방식으로 만들어 자동으로 정답 위치에 즉시 찍히게 한다.
9. **테스트** — 가짜 DOM에서는 애니메이션 검증이 불가능하다. 좌표 데이터 정합성과 채점 로직만 테스트하고,
   연출은 실기기 확인으로 남긴다.

**만 4세 관점의 현실적 목표** — "지구에는 땅 덩어리가 여섯 개 있고 케냐는 저 아래쪽"까지다.
정확한 국가 위치가 아니라 대륙 수준의 인식을 채점하는 편이 안전하다.

항목 4·7·8의 구체 제약은 [MAP-RENDER-CONSTRAINTS.md](MAP-RENDER-CONSTRAINTS.md) 에 있다.

## D4 — 194개국 전부 완성 후 공개 (권장안과 다름)

대륙 단위 점진 공개 대신 전량 완성 후 한 번에 낸다.

**이 선택의 대가**

- 몇 주간 민규에게 아무 변화가 없다. 그동안의 동기는 기존 5개 모드가 감당해야 한다.
- 화풍이 안 맞는 걸 늦게 발견하면 재작업 범위가 전량이다. → **D7의 파일럿 비교가 그만큼 더 중요해진다.**
  파일럿 8종을 A/B로 뽑아 민규 반응까지 확인한 뒤에 본 생성에 들어간다.
- 194행 주제 표를 먼저 확정하고 아빠가 승인(행당 5초, 약 20분)한 뒤에 그림을 만든다.
  사실 오류와 소재 부적절은 그림이 아니라 주제 한 문장에서 결정된다.

**완화책** — 기능 플래그로 미완성 콘텐츠를 숨긴 채 코드는 계속 `main`에 넣는다.
플래그 값은 최상위 스칼라가 아니라 **객체 안에** 넣어야 한다(D6 참조).

## D5 — 나라당 1장 + 예비 2개

상징물 194장. 만 4세는 쌍연합학습으로 배우므로 나라:그림 = 1:1이어야 한다.
국기 194개가 전부 달라서 민규가 외울 수 있었던 것과 같은 이유다.

예비 2개는 **중복 충돌 해소용**이다. 사자가 스리랑카·케냐·에티오피아·싱가포르 넷에 걸리면 1:1이 깨지므로
우선순위가 높은 나라가 가져가고 나머지는 예비로 민다.

**선정 우선순위** — ①국기에 그려진 사물·생물(실측 36개국) ②공식 지정 국조·국화·국수·국목
③국장 요소 ④매일 먹고 입고 타는 것 중 세계적으로 1:1로 묶이는 것 ⑤나머지만 자연물.

**"유명한 것"이 아니라 "그 나라가 스스로 고른 것"**이 기준이다. 이것이 고정관념 방어 장치다.
~~아프리카 54개국에서 동물 카테고리는 10개국(18.5%) 이하로 제한한다.~~ → **D13에서 12개국(22%)으로 상향.**

**무기 제외 6개국** — 오만(단검)·사우디(칼)·앙골라(칼)·케냐(창)·바베이도스(삼지창)·모잠비크(소총).
국기 자체는 그대로 두되 상징물 카드로는 뽑지 않는다.

## D6 — 기록 축 분리 + 스티커 칸 도장

`recordAnswer`에 축 인자를 하나 추가해 국기·그림을 나눠 저장한다.
출제 가중치(`storage.js:119-126`)와 오답노트(`storage.js:112-117`)는 **국기 축만** 본다.

194칸 스티커판은 국기의 것으로 유지하고, 각 칸 안에 작은 도장 3개(그림·명소·위치)를 겹친다.
이미 다 채운 판이 다시 미완성이 되면서도 4세가 새 규칙을 배울 필요가 없다.

**구현 시 함정 3가지**

1. `localStorage` 키 `flagquiz.v1`을 **절대 올리지 않는다.** 마이그레이션 코드가 저장소 전체에 없다.
2. `storage.js:48-49`가 **객체인 값만** 복원한다. 최상위에 스칼라(`mapTourDone: true` 같은)를 넣으면
   저장은 되고 절대 읽히지 않아 매번 기본값으로 돌아간다. 반드시 객체 안에 넣는다.
3. 병합이 1단계뿐이라 기존 나라 레코드에 새 필드가 백필되지 않는다. 모든 새 카운터를 `(s.x || 0)`으로 방어한다.
   새 버킷은 `resetProgress`에도 함께 추가한다.

**착수 전 선행 작업** — 설정 화면에 "기록 내보내기" 버튼(약 30줄).
지금 민규 진행도의 백업본이 이 세상에 하나도 없다.

## D7 — B · 그림책 과슈 확정

2026-09-15 사용자가 판다·만리장성 A/B 비교 시안을 보고 “B로 해주세요”라고 선택했다. 이후 그림은 **B**를 기준으로 한다. 프리셋을 만들 때 이 프로젝트의 `styleChoice`에는 `b`를 반영한다. 아직 선택되지 않은 작업의 기본값 `null`과 null일 때 본 생성 거부 규칙은 유지한다. 두 후보의 전문은 [IMAGE-PROMPTS.md](IMAGE-PROMPTS.md)에 있다.

- **A · 납작한 색면** — 외곽선 없는 2톤 색면, 흰 배경 + 옅은 파란 원(`#EEF4FF`, 기존 `--bg` 토큰).
  이모지·국기 SVG와 같은 시각 어휘. WebP 압축이 잘 먹어 장당 20~40KB.
- **B · 그림책 과슈** — 갈색 외곽선 `#4A3B2F` + 종이 질감 + 좌상단 광원.
  외곽선 색·굵기·광원 방향이 눈으로 즉시 검증되는 수치라, 레퍼런스 이미지나 시드 고정을 못 쓸 때 드리프트를 잡기 쉽다.

당초 계획은 같은 소재 8종을 양쪽으로 뽑아 아이패드에서 비교하는 것이었다. 사용자의 이번 선택으로 화풍 결정은 완료했지만, 정식 16장 파일럿·소재 전수 승인·앵커 승인·본 제작 완료를 의미하지는 않는다.
소재 8종은 세로로 긴 것, 개수가 사실인 것, 글자가 들어가기 쉬운 것, 동물, 먹을거리,
민규가 확실히 아는 나라, 어려운 나라, 상징물과 명소가 둘 다 있는 나라를 각각 검증하도록 고른다.

---

## 아직 열려 있는 것

- ~~**퀴즈 모드의 손가락 조작 방식** — 2주 뒤 모드 작업 때. 현재 권장은 기존 사지선다 재사용.~~ → 기존 사지선다 재사용으로 구현됐다([2026-09-17 이후 확인된 결정](#2026-09-17-이후-확인된-결정)).
- ~~**지도 퀴즈가 무엇을 채점하는가** — 정확한 국가 위치인지 대륙 수준인지.~~ → 나라 단위 핀 4지선다로 구현됐다(같은 절).
- **쓰실 도구의 능력** — 4:3 가로 출력, 배치 생성, 레퍼런스 이미지 첨부, 시드 고정 가능 여부.
  첫 한 장을 뽑아 보면 확인된다.
- **맥의 WebP 변환 도구 — 확인됨(2026-09-15)**: sips-316은 WebP 읽기 전용, cwebp 1.6.0은 쓰기 가능. ffmpeg·magick·convert는 PATH에 없다. 다른 환경에서는 재확인한다([STATUS.md](STATUS.md)).
- **생성 도구 약관의 재배포 권리와 출처 표시 의무** — MIT 저장소에 넣기 전에 확인이 필요하다.

---

# 교정 1차 이후 추가 결정

[FIX-ROUND1.md](FIX-ROUND1.md)의 결과를 보고 내린 결정이다.

## D8 — 명소 축의 1:1을 완화한다

교정 1차가 "나라:그림 1:1"을 명소 축에도 엄격히 적용한 결과 명소 칸이
144(74.2%) → 114(58.8%)로 30칸 줄었다. 아프리카는 오히려 51.9% → 50.0%로 내려갔고
북아메리카는 56.5% → 43.5%로 전 대륙 최저가 됐다.

원인은 구조적이다. **만 4세가 알아보는 사물의 가짓수가 194보다 훨씬 적다.**
화산 6개국·폭포 7개국·유럽풍 구시가지 9개국은 사실 서술이라, 바꾸면 거짓이 되고 비우면 칸이 사라진다.

**그래서 명소 축에서는 같은 범주도 전부 그린다.** 대신 두 가지를 한다.

1. **구별 지침을 문자열에 못박는다** — 색·형태·소품으로 4세 눈에 무엇이 다른지 명시한다.
   좋은 예: "플리트비체 — 에메랄드빛 물이 층층 계단을 이루며 나무다리가 물 위를 가로지름"
   나쁜 예: "아름다운 폭포"
2. **혼동군을 같은 판에 내지 않는다** — `quiz.js`의 오답 생성에 배제 규칙을 넣고 테스트로 고정한다.

**완화가 덮지 못하는 것** — "겹쳐도 그린다"이지 "안 보여도 그린다"가 아니다.
저작권으로 비운 6칸(my·ae·kw·sg·ec·am)과 한 나라 안 상징물=명소 중복으로 비운 4칸(kh·by·lc·dj)은 되살리지 않는다.
4세에게 형태가 읽히지 않는 칸(규칙 6)도 여전히 비운다.

**상징물 축의 1:1은 그대로 엄격히 지킨다.** 예비(벤치)도 마찬가지다 — 승격되는 순간 1:1이 깨지면 안 된다.

## D9 — 상징물의 규칙2 양보 5건을 그대로 둔다

중복을 푸는 과정에서 다섯 나라가 "그 나라가 스스로 고른 것"(국기·국장)을 포기했다.

| 나라 | 원래 (우선순위) | 바뀐 것 |
| --- | --- | --- |
| 리히텐슈타인 | 국기의 금관 (1순위) | 우표 |
| 북마케도니아 | 국기의 해 (1순위) | 빨간 고추 |
| 몰디브 | 국장의 야자 (3순위) | 만타가오리 |
| 투발루 | — | 소라 조개껍데기 |
| 폴란드 | 국장의 독수리 (3순위) | 만두(피에로기) |

만 4세에게는 "그 나라가 고른 것이냐"보다 **"그림이 서로 다르냐"가 훨씬 중요하다.**
국기 194개가 전부 달라서 민규가 외울 수 있었던 것과 같은 이유다.

## D10 — 종교 건축물 7건을 외관만 그려 남긴다

va 성 베드로 · ru 성 바실리 · es 사그라다 파밀리아 · bg 릴라 수도원 · ml 젠네 · si 블레드 · ge 게르게티

성상·의식·신도·내부 없이 **건물 외곽 실루엣만** 그린다. 각 명소 문자열에 이 제한을 명시한다.

단 바티칸은 상징물마저 교황 열쇠(종교 표장)라 카드 두 장이 모두 종교가 된다.
**명소(성 베드로 외관)는 남기고 상징물만 다른 소재로 바꾼다.**

---

# 최종 정리 이후 추가 결정

[FINAL.md](FINAL.md)의 "아빠 판단이 필요한 것"에 대한 답이다.

## D11 — 아프리카 명소를 한 번 더 발굴한다

아프리카가 51.9%(28/54)로 전 대륙 최저다. 아시아 80.4%·유럽 84.4%와 비교하면
이대로는 아이가 "아프리카는 볼 게 없는 곳"이라고 배운다.

**기준을 바꿔서 다시 훑는다** — "세계적 유명세"가 아니라 **"그림으로 그렸을 때 다른 카드와 구별되는가"**.
자연물(특이한 나무·바위·호수·폭포·사막·화산·동굴·섬)과 사람이 만든 것(전통 마을·흙집·시장·배)을 폭넓게 본다.

단 억지로 채우지 않는다. 지금의 아프리카 표는 보유한 28개 중 25개가 S·A급인
"쓸 만한 것만 남긴 정직한 표"다. 그 품질을 떨어뜨리면 안 된다.

## D12 — 모자 6중만 재배치하고 나머지 둘은 유지한다

상징물 축에 "문자는 다르지만 4세 눈에는 같은 그림"이 3건 남아 있었다.

| 중복 | 처리 |
| --- | --- |
| 모자 6중 (ls·kg·vn·pe·co·cg) | **한 나라만 남기고 다섯에 새 상징물** |
| 리비아 대추야자 열매 송이 ↔ 시에라리온 기름야자 열매 송이 | 유지 — 색으로 갈린다(호박색 ↔ 주황빛 빨강). 프롬프트에 색을 못박고 혼동군 등록 |
| 이스라엘 사해 소금 ↔ 파키스탄 분홍 소금 | 유지 — 색으로 갈린다(하양 ↔ 분홍). 볼리비아 우유니 명소와 함께 혼동군 등록 |

모자는 여섯 장이 정말로 구별되지 않지만, 나머지 둘은 색 하나로 갈린다.

## D13 — 아프리카 동물 한도를 12개국(22%)으로 올린다

10개국(18.5%)은 근거 있는 선이 아니라 제안된 숫자였고, 여유가 0이라
예비가 승격하는 순간 규칙이 깨지는 상태였다(그런 칸이 8개 있었다).

12개국이어도 고정관념 방어라는 목적은 달성된다 — 아프리카 54개국 중 동물이 22%면
나머지 78%가 식물·먹을거리·만든것·입는것·땅과하늘로 채워진다.

## D14 — 시리아 크락 데 슈발리에를 뺀다

분쟁지 문화재다. 같은 사유로 팔미라가 이미 빠져 있어 기준을 맞춘다.
이 저장소는 이미 `README.md:173-176`에 "어른들 사이에서도 답이 갈리는 것은 아이 자료에서 뺀다"는
방침을 갖고 있고, 그 방침의 연장이다. 시리아는 상징물(알레포 비누)로 카드가 선다.

## 답 없이 이대로 가기로 한 것

- **유럽 84.4%** — 60~80% 밴드는 감사가 스스로 세운 목표이지 요구사항이 아니다. 맞추려고 다시 비우는 것은 D8과 충돌한다.
- **카자흐스탄 야사위·우즈베키스탄 레기스탄·태국 왓 아룬** — D10이 열거한 7건 밖이지만, 표에 이미 미얀마 쉐다곤·인도네시아 보로부두르·부탄 탁상 사원이 같은 지위로 있다. D10의 외관-only 문구를 문자열에 못박아 채택한다.
- **바티칸 상징물** — 교황 열쇠(종교 표장)에서 박물관 나선 계단으로 바꾼다.
- **벤치 깊이** — 예비가 1칸인 나라가 87개국이 됐다. 예비는 충돌 해소용이므로 1칸이어도 기능한다.

---

# 2026-09-17 이후 확인된 결정

위 결정과 실제 구현이 다르거나 그 뒤에 정해진 것을 코드·커밋 기준으로 적는다. 결정을 되돌린 기록이 아니라 사실 기록이다.

| 관련 | 실제 |
| --- | --- |
| D2 "도감 먼저, 2주 뒤 모드 1개" | 도감 카드 그림(`js/ui.js` countryArt)과 **그림·명소 퀴즈 2모드**(`js/quiz.js` MODES `symbol`·`place`)를 2026-09-16 PR #12 로 **한 번에** 병합했다. 두 모드 모두 "그림 1장 → 국기 4개 중 고르기"라 D2 의 권장 조작(기존 사지선다 재사용)을 따른다. 처음 만나는 나라는 문제보다 먼저 **만나기 카드**(`js/app.js` needsMeet)가 그림·국기·이름을 보여 주고 "문제 풀어 볼게요"를 누르면 같은 나라를 문제로 낸다. |
| D1 후속 "명소 카드에 ○○의 수도는 △△예요 병기" | **미구현**. 그림·명소 정답 카드는 나라 이름 → 그림 이름 → 상식(`fact`) 순으로 읽고(`js/app.js` `[c.ko, feedbackArtName, c.fact]`), 수도는 도감 상세(`js/ui.js` countryModal 의 수도 줄)와 수도 모드가 담당한다. |
| D3 지도 채점 단위(열려 있던 것) | 대륙 수준이 아니라 **나라 단위**다. 국기와 이름을 보고 지도 위 핀 4개 중 그 나라 자리를 고르는 4지선다(`js/quiz.js` mode `map`, `FQ.map.chooseOptions`)이며 기록은 `axes.map` 에 국기 축과 분리해 쌓는다. |
| D4·7단계 "342개 전부 완성 후 공개" | 342개 중 기니 상징물(코라)만 `held` 인 상태로 먼저 `art` 를 켰다(51dda80 — 보류 소재는 이름만 남기고 그림 자리를 비운다). 2026-09-17 기니를 젬베 북으로 교체·승인해 342/342 를 채웠다(a74c445). |
| HANDOFF 금지 6 "새 수아 문구 0개" | 2026-09-17 사용자 결정으로 **상징물·명소 이름 342개**를 생성해 1,319문구가 됐다(d6f135e). 화면이 읽는 문구는 여전히 `js/voice-manifest.js` 에 있는 것만이며, 새 문구는 음원을 먼저 채운 뒤에만 병합한다. |
| D5·D13 자료 정합(2026-09-17 후속 정비) | `SUBJECTS.csv` 카테고리 12건을 소재에 맞게 정정했다 — ae 달라 주전자·do 야구 글러브·sy 알레포 비누 → 만든것, pl 만두·af 석류·mk 고추 → 먹을거리, gr 돌고래·hn 사슴·mv 만타가오리·to 고래 → 동물, lc 피통·tv 소라 → 땅과하늘. 정정 뒤 아프리카 동물 상징물은 **10/54(18.5%)** 로 D13 한도 12 안이다. 캐나다 명소도시를 옐로나이프(오로라)로, 명소도시가 수도와 같은 7행(az·th·ee·li·lv·se·cg)의 '수도에있음'을 Y 로 맞춰 수도 안 명소는 17 → **24**('수도 인근' 5행은 그대로 N). 소재 문장·화면 이름(koApprove)·음원은 바꾸지 않았다. |
| 오프라인 그림(2026-09-17 후속 정비) | 서비스워커 그림 예열이 워커 종료로 끊기면 다음 온라인 셸 응답에서 이어받고, 다 채운 목록을 그림 버킷 안 기록(`./images/_warm-art`)으로 남겨 되풀이 조회를 피한다. 같은 이름으로 교체된 그림은 `data/subjects.js` 에 실은 원장 `bytes` 와 크기가 다르면 HTTP 캐시를 건너뛰고 새로 받는다. 음원 버킷 `flagquiz-v4` 는 손대지 않는다. |

## 2026-09-17 후속 정비에서 정한 것 (에이전트 3인 검토)

- **D15 — 새 축 출제 규칙.** 그림·명소·지도는 "한 번 더 만날 나라를 자주" 설정 시 자기 축 기록만 읽는 가중치(`storage.axisWeightOf`, 레코드를 만들지 않음)로 순서를 정한다. 한 판 안에서 그 축에서 처음 만난 쌍과 틀린 쌍은 3문제 뒤에 한 번 더 낸다(총 문제 수 불변, 쌍당 한 판에 한 번, 마지막 두 문제에서는 잡지 않음 → 5문제 판까지는 재만남이 적고 10문제 판은 새 나라 5개를 두 번씩 만난다). 국기 축은 이 엔진에 들어가지 않는다(D6 유지).
- **D16 — 다시 살펴볼 나라 목록.** 그 축에 자료 없는 나라는 빼고(`game.skipped`), 전부 빠지면 전역 풀이 아니라 난이도·대륙 조건 풀로 가며 `game.fallback` 에 이유를 남긴다.
- **D17 — 채택(B): 수도 모드는 `capital` 축 + 도감 4번째 🏙️ 도장(2026-09-17 아이 기준 시안 PhoneCapital).** `js/quiz.js` `MODES.capital.axis = 'capital'`, 기록은 `axes.capital` 버킷. 놀이 자체를 뒤집어 **수도 이름을 듣고 국기 4장에서 나라를 찾는다**(라벨 '수도 듣고 국기 찾기'): 문제가 뜨면 수도 이름 음원(`c.capital`)을 한 번 자동으로 읽고 큰 🔊 로 다시 듣는다, 보기는 그림 놀이와 같은 국기 격자(`.answer-grid.art-grid`), 채점은 나라 code. 힌트는 국기 힌트(=정답) 대신 '🗺️ 대륙 · 지역에 있어요' + 오답 2개 지우기 + 수도 다시 읽기. 정답 카드는 국기 + 나라 이름 + '🏙️ 수도 · 나라의 수도예요'(읽는 문구 `[c.capital, c.ko + '의 수도예요']` 불변). 처음 만나는 나라는 만나기 카드(국기·나라·수도, 같은 두 문구, 기록·제한 시간 없음). 새 축 규칙(D15 가중치·다시 만나기, D19 대결 없음, D20 시간 초과 지나감, 오늘의 도전·194칸 스티커·배지 미집계)을 그대로 받는다. 새 수아 문구는 없다. 보기가 국기라 '같은 수도 두 번 금지' 특례는 없앴다(수도 이름이 겹치는 나라는 자료에 없다). **옛 기록은 그대로 둔다** — 분리 전 수도 정답이 `countries` 에 섞여 있어도 되돌릴 수 없고, 되돌리지 않는다(스티커를 뺏지 않는다).
- **D18 — 홈 모드 카드.** '🚩 국기 놀이' 5장 / '🎨 그림·지도 놀이' 3장 두 묶음, 묶음 안 순서 고정(비문해 아이는 자리로 카드를 기억), 놀이 카드가 '누가 하나요?' 위. 그림을 끄면 두 번째 묶음은 '🗺️ 지도 놀이'.
- **D19 — 둘이서 대결은 국기 축에서만.** 그림·명소·지도에서는 스위치를 감추고 켜져 있어도 첫 사람 혼자 논다(INTERVIEW 325~326). 저장된 두 이름은 지우지 않는다.
- **D20 — 미채점 '지나감'.** 그림 로드 실패로 건너뛴 문제와 새 축의 제한 시간 초과는 기록·총 문항·정답률·놀이 기록·만점 배지·대결 차례 어디에도 들어가지 않는다. 국기 축의 시간 초과는 종전대로 채점. 결과 화면 '오늘 만난 나라'는 문제 수가 아니라 실제 채점한 나라 수.
- **D21 — 힌트.** 그림·명소 힌트의 소재 이름 줄은 나라 이름(별칭 포함)이 들어 있으면 뺀다(lb·cy·mc·cm·mw·pa). 수도 힌트는 초성 대신 국기 특징 + 대륙·지역.
- **열린 결정.** 새 축에서 제한 시간 스위치 자체를 숨길지(INTERVIEW 권장). 지금은 스위치는 두고 초과만 지나감으로 처리한다.
- **검사기 주의(2026-09-17 완화).** `scripts/verify-expansion.mjs` 의 `css/style.css` 인용 검사는 이제 줄 번호가 아니라 `prefers-reduced-motion` 규칙의 존재만 본다. 다른 인용(SPEC.md·effects.js·index.html)은 여전히 줄 번호 ±3행이다.

- **D22 — 깜짝 상자(2026-09-17).** 카드 5장마다 100% 열리던 여행 상자를 깜짝 선물로 바꾼다. 지난 상자 뒤 2장째부터 매 문제 15% 확률, 8장째에는 반드시(평균 5.5장, 전보다 10% 뜸함). 빈 상자는 없다. 모양은 방금 만난 나라의 대륙(연등·리본 상자·북·풍선·피냐타·조개)이고 상자 음악 6곡을 대륙에 하나씩 붙인다. 셋 중 하나를 골라 열고 열에 셋은 한 번 더 두드린다. 종류는 보통 75%(5점·경험치 20)·반짝 20%(경험치 40)·황금 5%(10점·경험치 60). 안에서 방금 만난 나라의 국기(그림 놀이면 그림)가 친구 카드로 나온다. 지난 상자 뒤 카드 수는 `storage.chest` 버킷에 저장해 앱을 다시 열어도 8장 보장이 이어진다. 여행 카드 칸 다섯 개는 카드 모으기 표시로만 남는다.
- **D23 — 제한 시간은 국기 놀이에만(2026-09-17).** 그림·명소·지도·수도 놀이는 아이가 처음 보는 것이 많아 초시계를 붙이지 않는다(INTERVIEW 권장). 아빠 설정의 제한 시간 줄은 국기 놀이를 고른 채 열 때만 보이고 저장값은 그대로 둔다. 시간 초과 지나감(D20)은 국기 축 밖에서는 더 이상 일어나지 않는다.
- **D1 후속 이행(2026-09-17).** 명소 정답 카드에 '🏙️ 수도 · ○○의 수도예요' 한 줄과 듣기 단추(기존 음원 두 문구)를 붙였고, 도감 상세의 수도 줄에도 듣기 단추를 두었다. 자동으로 읽지는 않는다(정답 낭독을 짧게 유지).

## 2026-09-18 수도 놀이 학습 엔진

- **D24 — 수도 놀이의 출제는 D15 대신 `LEARN.capital`(`js/quiz.js`) 규칙을 탄다.** 그림·명소·지도는 D15 그대로다. 아빠의 관찰("게임으로 공부하듯 하는데 더 쉽게 익히게")과 진단 — 기본 난이도 '쉬움' 49개국에 미만남 가중치가 두 배라 매 판 새 나라 위주로 나와 "넓게 얕게" 흐르고, 오답 보기가 같은 세부 지역부터라 처음 배우는 아이에게 최고 난도이며, 힌트가 글자라 비문해 아이에게 닿지 않는다 — 에 대한 답이다. 네 가지를 한 묶음으로 바꿨고 새 수아 문구는 없다.
  1. **새 나라 한도.** 한 판에 처음 만나는 나라는 문제 수에 따라 5문제 2개 · 10문제 3개 · 20문제 4개 · 전부 5개까지만 무작위로 뽑는다. 남는 자리는 이미 만난 나라의 복습이고, 복습 순서 가중치(`reviewWeight`)는 틀렸고 안 굳은 나라 3.0 · 한 번 맞힌 나라 2.0 · 2~4연속 1.4 · 5연속 이상 0.6 이다('한 번 더 만날 나라를 자주'를 끄면 복습은 무작위). 복습이 밀리면 — 가중치 2 이상인 안 굳은 나라가 한도의 두 배(10문제 6개) 이상이면 — 새 나라를 하나 줄인다. `storage.axisWeightOf` 는 다른 축을 위해 그대로 둔다.
  2. **소개 → 1문제 뒤 → 3문제 뒤 리듬, 복습이 없으면 판 끝에 한 번 더.** 한 판의 순서를 미리 계획한다(`planSession`). 새 나라는 소개 뒤 1문제 뒤와 3문제 뒤에 다시 나오고, 복습할 나라가 없어 자리가 남으면 판 끝에 한 번 더 나온다(10문제 새 3개에 복습 둘이면 첫 나라는 0·2·6번째, 복습이 없으면 0·2·5·9번째). 복습이 판 끝 다시 만나기보다 먼저다 — 어제 만난 나라를 오늘 흔드는 쪽이 오래 남는다. 같은 나라가 연달아 나오지 않고, 총 문제 수는 count 를 넘지 않으며 낼 것이 없으면 짧아진다(나라 하나만 고른 '다시 살펴볼 나라'는 한 문제). 틀렸거나 힌트 2단계까지 쓴 문제는 채점 때 1문제 뒤에 한 번 더 내되 복습 자리 하나를 뒤에서 빼서 쓰고(총 문제 수 불변, 사이에 낄 문제가 없으면 안 냄), 한 나라는 한 판에 다섯 번까지다. 처음 만난 나라의 만나기 카드(D17)는 소개 문제 앞에 그대로 뜬다.
  3. **보기 거리는 익힌 정도로.** 보기(국기 3장)는 문제가 화면에 오를 때 그 축 기록으로 만든다 — 연속 정답 0~1이면 다른 대륙 국기부터(`far`), 2~4면 같은 대륙·다른 지역부터(`mid`), 5부터 같은 세부 지역부터(`near`, 국기 놀이의 원래 규칙). 대륙을 골라 놀면 다른 대륙이 없어 한 단계 물러난다. `distractors` 의 `opts.spread` 이며 다른 모드는 `near` 그대로다.
  4. **소리 힌트 2단계.** 1단계는 오답 2개 지우기 + 수도 이름 다시 읽기(대륙·지역 글자는 옆의 어른 몫), 단추가 '🔊 나라 듣기'로 바뀌고 2단계는 '○○의 수도예요'(나라 이름)까지 읽는다. 나라 이름을 들으면 국기는 100% 찾으니 늘 성공으로 끝나되(무오류 학습), **아이에게는 정답(점수·발견 카드)이고 기록에는 틀림**으로 남긴다(`submit` 의 `opts.revealed` → `res.learned=false`, `recordAnswer(…, false)`, '한 번 더 만날 나라'와 여행 카드 '한 번 더 만나요'). 국기·그림·명소·지도 힌트는 종전대로 한 번이다.
  - **바꾸지 않은 것.** 저장 키·버킷(`axes.capital`)·읽는 문구·만나기 카드·정답 카드·도감 도장 기준(첫 정답)·D19 대결 없음·D20 지나감·D23 제한 시간 없음. 도장 3단계·수도 안 명소 그림·'오늘 만난 수도 다시 듣기'·소개 순서 큐레이션·국기 보고 수도 말하기는 다음 묶음으로 남겼다.

## 2026-09-18 말로 답하기

- **D25 — 말로 답하기: 소리 닮은꼴·앞부분 인정, 확실한 중간 결과 즉시 채점, 맞히면 저절로 다음.** 아빠의 관찰("말로 할 때 잘 못 알아듣는다", "맞히면 누르지 않아도 넘어가게")에 대한 답이다. 실기기 인식 로그가 없어 놓치는 길 세 갈래(엄격한 짧은 이름, 최종 결과만 기다리기, 긴 이름 끝까지 말하기)를 함께 손봤다. 새 수아 문구는 없다.
  1. **목표 나라만 소리 기준으로 한 번 더 본다**(`quiz.soundsLike`, `util.soundKey`). 초성 ㄱ·ㅋ·ㄲ / ㄷ·ㅌ·ㄸ / ㅂ·ㅍ·ㅃ / ㅈ·ㅊ·ㅉ / ㅅ·ㅆ, 중성 ㅐ·ㅔ / ㅒ·ㅖ / ㅙ·ㅚ·ㅞ / ㅢ·ㅣ, 받침 일곱 소리를 같은 소리로 본다('캐냐'→케냐, '구바'→쿠바, '패루'→페루). 허용 오차는 엄격 채점과 같아 두 음절 이름은 소리 묶음이 정확히 같아야 한다('하나'는 가나가 아니다). 세 음절 이상을 말했고 그것으로 시작하는 이름이 그 나라뿐이면 앞부분만으로도 인정한다('사우디아라'→사우디아라비아, '오스트레일리'→오스트레일리아; '도미니카'는 공화국·연방이, '오스트'는 오스트리아·오스트레일리아가 겹쳐 안 되고 '사우'는 두 음절이라 안 된다). 다른 나라 이름이 낱말 그대로 걸리면 이 단계를 밟지 않고, 비슷하게만 걸렸을 때는 목표 나라가 소리로 정확히 같을 때만 인정한다('부단'→부탄, '수단'은 수단, '수담'은 수단으로 알아들음). 니제르≠나이지리아 같은 기존 부정 검사에 더해 194개국 전수 교차 검사(다른 나라의 어떤 이름·별칭도 인정되지 않음)로 고정했다. `checkText` 결과의 `lenient` 가 표시다. 쓰기 모드도 같은 채점을 쓴다.
  2. **중간 결과라도 목표 나라 이름이 확실하면 바로 채점한다.** 최종 결과를 기다리다 사파리가 스스로 끊어 놓치는 일을 줄이고 아이는 말하자마자 카드를 본다. 이름이 다른 나라 이름의 앞부분인 나라(`quiz.prefixRisky`: 인도·기니)는 종전대로 끝까지 듣는다 — "인도네시아"의 "인도"를 먼저 채점하지 않는다. 다른 나라 이름은 중간 결과로 채점하지 않는다.
  3. **말로 맞히면 설명 낭독이 끝난 0.9초 뒤 저절로 다음 나라로 간다**(읽어주기가 꺼져 있으면 1.8초 뒤). 틀리면·깜짝 상자가 열리면·낭독이 실패해 '눌러서 들어보기'가 뜨면 종전대로 아이가 누른다. '설명 다시 듣기'를 누르면 예약이 취소되고 다시 들은 뒤에 넘어간다. 화면을 벗어나면 예약도 사라진다. 다른 놀이는 바뀌지 않는다 — 정답 카드를 보고 스스로 넘기는 리듬을 유지한다.
