# FlagQuiz 확장 진행 상태

갱신: **2026-09-17 KST**. 아래는 현재 상태이며, 이전 단계별 보고는 Git 이력과 [기존 검증 기록](verification/)에 보존된다.

## 현재 결론

- **B 화풍 최종 그림 342/342개**: 상징물 194/194, 대표 명소 148/148. 기니 상징물은 코라 보류 이력을 보존한 채 젬베 북으로 교체했고, 독립 시각 검수·WebP 변환·전량 파일 검사를 통과했다. 보호자 개별 승인이나 아이 인지 검사를 했다는 뜻은 아니다.
- **나라 위치 핀·상징물·명소 퀴즈 구현 완료**. 지도는 194개 핀과 402개 육지 서브패스이며, 새 축의 정오답 기록은 기존 국기 기록과 분리한다.
- **그림 공개 상태**: `js/features.js` 기본값 `art: true` 에서 `noArt` 소재는 0개이며, 전량 필수 게이트 `FQ_REQUIRE_ART=1`도 통과한다. 기니의 최종 경로는 `images/symbols/gn.webp`다.
- **병합·배포**: `claude/flag-game-expansion-design-rpv24x` 는 [PR #12](https://github.com/techjuicelab/FlagQuiz/pull/12)로 2026-09-16 main 에 병합됐다. 기니 젬베 커밋 `a74c445`의 [GitHub Pages 배포](https://github.com/techjuicelab/FlagQuiz/actions/runs/35167904817)는 성공했고, 공개 `gn.webp`는 HTTP 200·`image/webp`·19,382 bytes·승인 SHA로 응답했다.
- **2026-09-17 후속 정비**: 도감 카드의 깨진 그림이 css `display: block` 에 밀려 그대로 보이던 것을 인라인 style 로 숨겼고, 서비스워커 그림 예열이 워커 종료로 끊기면 다음 온라인 셸 응답에서 이어받으며 같은 이름으로 교체된 그림을 원장 `bytes` 로 알아채 새로 받게 했다(음원 버킷은 그대로). `SUBJECTS.csv` 카테고리 12건·캐나다 명소도시·'수도에있음' 7행을 정정했고(수도 안 명소 17 → 24, 아프리카 동물 10/54 로 D13 유지), 결정과 구현의 차이는 [DECISIONS.md](DECISIONS.md) 끝의 "2026-09-17 이후 확인된 결정"에 적었다.
- **2026-09-17 아이 기준 시안 · 수도 놀이 뒤집기(D17 채택 B)**: 수도 놀이가 '수도 듣고 국기 찾기'가 됐다 — 큰 🔊 가 수도 이름을 자동으로 읽고 국기 4장에서 나라를 찾으며, 기록은 `axes.capital` 로 분리돼 도감 칸의 4번째 🏙️ 도장이 됐다(그림·명소·위치·수도). 새 축 규칙(가중치·다시 만나기·시간 초과 지나감·대결 숨김·오늘의 도전 미집계)을 받고, 새 수아 문구는 없다. 옛 수도 기록(국기 `countries` 에 섞인 것)은 그대로 둔다.
- **2026-09-17 추가 작업**: 기니 상징물을 젬베 북으로 교체해 최종 승인했다. 후보 3회 중 앞의 2회는 채택하지 않았고, 3번째 후보만 열린 하단·단일성·무글자·무국기·무인물·약 70% 높이·4:3·단일 하단 그림자를 교차 검수해 채택했다. 수아 음원에 상징물·명소 이름 342개를 추가해 총 1,319문구가 됐고(HANDOFF 금지 6 해제), 그림 문제·정답 카드·도감에서 그림 이름을 읽어 준다. 깜짝 상자(D22): 5장 고정 상자를 확률형(2장째부터 15%, 8장 보장)·대륙 모양·셋 중 하나 고르기·3등급으로 바꿈. 제한 시간은 국기 놀이에만(D23), 명소 카드·도감에 수도 듣기 한 줄(D1 후속).

## 남은 일

| 항목 | 상태와 다음 조건 |
| --- | --- |
| 기니 그림 1개 | 완료. 2026-09-17 사용자 결정으로 소재를 **젬베 북**으로 교체했고(SUBJECTS.csv·presets.json·settings.csv·prompts/gn-symbol.txt), `approved-text` → 원본 보존 → `images:prepare` → `approved-image` 기록 → subjects/contact/prompts 재생성 → 전수 검사 순으로 마쳤다. 코라는 예비1 및 실패 5회 이력으로 보존한다. |
| 그림 전량 공개 | 완료. `art` 기본값은 이미 켜져 있고, 기니 `noArt`를 해제해 342개 필수 게이트·엄격 빌드·실제 브라우저 GN 디코딩을 통과했다. `a74c445`를 main에 반영하고 GitHub Pages 공개 자산의 HTTP 200·`image/webp`·19,382 bytes·SHA 일치까지 확인했다. |
| 아이패드 실기기 | 설치형 Safari PWA에서 비행기 모드 앱·국기·수아 재생, 기존 114MB 음원 재다운로드 없음 확인이 남았다. macOS 브라우저 검증으로 완료 처리하지 않았다. |
| 보호자·아이 검수 | 소재 문장·B 앵커·4세 인지 및 선호 확인은 수행하지 않았다. 원장에 `humanApproval: false`로 기록했다. |

## 단계별 구현

| 단계 | 구현한 것 | 남은 것 |
| --- | --- | --- |
| 0 안전망 | 기록 JSON 내보내기, 캐시 네 버킷, v4 음원 보존, 국기 이관 실패 시 레거시 오프라인 폴백, 그림 예열 재개·완료 기록·크기 비교 갱신(2026-09-17) | 아이패드 실기기 확인 |
| 1 자료·스위치 | 기능 플래그, 194개국 주제 자료, 58개 혼동군, 라벨·문서 정정 | 없음 |
| 2 엔진 | 국기/상징물/명소/지도 기록 분리, 혼동군 배제, 자료 필터를 후보와 폴백 모두에 적용, 완비·고아 검사 | 없음 |
| 3 도감 | 상징물·대표 명소 그림, 오류 폴백(깨진 그림은 인라인 style 로 숨김), 축별 도장·기록, 조회 시 빈 기록 생성 방지 | 실제 아이 인지 확인 |
| 4 제작 도구 | CSV·원장·문장 lint·프롬프트 조립·오프라인 보드·WebP 변환·파일 검수·컨택트시트·후속 회차 기록 | 없음 |
| 5 소재·파일럿·앵커 | 342개 문장 개별 작성 및 에이전트 교차 검토, 사용자 B 선택 반영, B 8종 파일럿 검수와 앵커 1장 등록 | 보호자·아이 평가 |
| 6 그림·앱 배선 | 342개 B 그림, 생성·수정 이력, 소재 이름 연결, 그림 로딩 전 타이머·채점 차단, 오류 재시도, 지도 핀 선택 퀴즈 | 실제 아이 인지 확인 |
| 7 전량 공개 | 342개 필수 게이트·엄격 빌드·GN 실제 브라우저 디코딩·main GitHub Pages 공개 응답 | 없음 |
| 지도 독립 트랙 | 원자료 획득·조인·공유 경계 단순화·핀 보정·정합성·출처·44px 터치 핀 | 실기기 사용성 |

사용자의 후속 지시인 전체 구현·B 선택·나라 위치 핀 선택에 따라 구현과 제작을 진행했다. 과거의 '1~4단계 미착수', '342장 생성 금지', '화풍 미정' 상태는 현재 진행 상태가 아니다. 승인받지 않은 실기기 확인이나 보호자 검수를 완료로 바꾸지는 않았다.

## 그림과 제작 근거

- 최종 WebP **342개, 8,497,504 bytes**, 가장 큰 파일 60,726 bytes. 전부 B 규격 **1024×768**, quality 80, 좌우 12% 여백. 기니 젬베는 19,382 bytes이며 기계 검사 경고·실패 0이다.
- [전체 그림 검수판](../image-prompts/contact/1.html): 40개씩 9페이지. 밝은/어두운 배경과 기존 국기 옆 비교.
- [원장](../image-prompts/presets.json), [제작 기록](../image-prompts/production.json), [실제 생성 요청 387개](../image-prompts/generation-prompts/), [제작 출처](../image-prompts/PROVENANCE.md).
- 원본·native·최종 파일 **1,026개**의 해시와 실제 프롬프트 내용·순서·시도 수를 독립 대조했다. 새 파일이나 수정된 프롬프트가 승인 해시와 다르면 테스트·배포 검사가 실패한다.
- [B 파일럿 검수](../image-prompts/PILOT-REVIEW.md), [앵커 메타데이터](../image-prompts/anchor-review.json). B는 사용자가 확정했으므로 A 8종을 추가 생성하지 않았다. 앵커는 기존 광화문 후보 1장을 검수해 등록했으며, 여러 후보를 비교하거나 생산 전에 기준 그림을 확정한 것처럼 기록하지 않았다.
- 엄격한 원본 배경색 항목은 B 8종 모두 RGB 2~5 정도의 편차·질감 때문에 불일치했다. 전체 64항목 중 56개 통과이며, 이 사실을 감추거나 픽셀 단위 색 일치를 주장하지 않았다. 최종 여백 색은 지정 배경색으로 채웠다.
- 이번 342개는 내장 image_gen의 항목별 독립 호출로 제작했다. 과거를 정규 회차·앵커 재생성 로그로 소급 작성하지 않았으며, 기니의 젬베 생성 요청 3건과 채택·비채택 이력도 원장에 남겼다.
- 실패 원본도 로컬 `docs/artifacts/art-production-2026-09-15/`, `docs/artifacts/art-production-2026-09-17/`와 생성 도구 원본 폴더에 보존했다. Git에는 최종 WebP와 생성 요청·검수 이력을 넣는다.

## 후속 정비 검증 — 2026-09-17 (d6f135e 이후 작업 트리)

| 실행 | 결과 |
| --- | --- |
| `npm test` | 자료·통합 단언과 node 테스트 **284개** 통과, 실패·건너뜀 0 (서비스워커 예열 재개·크기 갱신·파서 5건, 도감 onerror 1건 추가) |
| `npm run verify` | **38 통과 · 0 실패 · 4 미실행**(`--deep` 전용) — 기준 38710d8 과 같다 |
| `npm run images:lint` | 오류 0 · 경고 0 · 미작성 0 — 카테고리·inCapital·city 메타만 바꿨고 koApprove·subjectEn·status·tries·bytes 는 그대로다 |
| `npm run subjects:build` | 나라 194 · 명소 148 · 혼동군 58. `data/subjects.js` 에 원장 `bytes` 가 실렸다(서비스워커 크기 비교용) |

## 최종 검증 — 제품 기준 38710d8

| 실행 | 결과 |
| --- | --- |
| `npm test` | 자료·통합 단언 **10,220건**, node 테스트 **265개** 통과, 실패·건너뜀 0 |
| `npm run verify` | **38 통과 · 0 실패 · 4 미실행**. 출력의 '미구현' 4개는 `--deep`에서 실행하는 검사 |
| `npm run verify -- --deep` | **42 통과 · 0 실패 · 0 미구현** |
| `npm run build` | 성공. Sua 음원 1,319개·음악 16개 준비 완료 |
| `npm run images:lint` / `images:check` | 성공. 최종 파일 342개·승인 342개·헤더 342개, 실패 0·경고 0 |
| 전량 필수 빌드 | `FQ_REQUIRE_ART=1 node scripts/build-site.mjs` **성공**. 342개 그림·1,319개 Sua 음원·음악 16개를 포함한 배포 파일을 만들었다. |
| 실제 브라우저 전수 로딩 | 기존 341개 전수 `Image.decode()` 성공에 더해, GN 최종 WebP도 격리 Chrome의 `HTMLImageElement.decode()`에서 **1024×768**으로 성공했다. |
| GitHub Pages 공개 자산 | `a74c445`의 배포 워크플로 test·deploy 성공 후 `https://techjuicelab.github.io/FlagQuiz/images/symbols/gn.webp`가 HTTP **200**·`image/webp`·**19,382 bytes**·SHA `cb715b…6572f`로 응답했다. |
| 지도 재현 | 바다 핀 **10 → 0**, 면적 0 서브패스 **1 → 0**. 194개 핀·402개 서브패스 |

요청한 검사 원문을 요약 없이 저장했다: [npm test](verification/2026-09-16-completion/npm-test.txt), [기본 verify 전문](verification/2026-09-16-completion/verify.txt), [심화 verify 전문](verification/2026-09-16-completion/verify-deep.txt), [전체 증거 안내](verification/2026-09-16-completion/README.md).

### 실제 브라우저·오프라인 확인 범위

격리된 Chromium 세션 390×844에서 홈 모드 선택과 실제 정답·오답 클릭을 검증했다. 테스트 서버를 실제 종료한 뒤 새 탭에서 앱·지도·받아 둔 그림·국기를 다시 열었다. 수아 음원은 실제 재생 완료했으며 Range 요청의 206/416도 확인했다. 명소 오답이 기존 국기 기록·오답노트에 들어가지 않았다.

이 검사의 앱 기준은 `e0faf8a`이며, 캐시된 app/quiz/storage/features/map 소스 해시가 당시 primary와 같았다. 후속 도감 조회 수정은 별도 회귀 테스트에서 검사했다. 전체 341장 사전 다운로드나 실제 아이패드 114MB 이관을 시험한 것은 아니다.

## 금지 사항 보존

1. 저장 KEY **`flagquiz.v1`** 유지.
2. 새 상태는 `settings.dev`와 `axes` 객체에 넣고, 누락 카운터 읽기·초기화 방어.
3. 음원 캐시 **`flagquiz-v4`** 유지. activate에서 음원 삭제·복사·재다운로드 없음.
4. 출제 후보와 전체 폴백 모두 자료 있는 나라만 허용. 완화하는 것은 혼동군 배제뿐.
5. 그림은 **`images/symbols/`·`images/places/`**, 국기 194개 SVG는 기존 `flags/` 유지.
6. 새 수아 문구 없음 — 2026-09-17 상징물·명소 이름 342개를 음원부터 채워 1,319문구로 늘린 뒤로는 `js/voice-manifest.js` 에 있는 문구만 화면에서 읽는다. npm 의존성 추가 없음.

과제별 커밋 전에 HANDOFF 금지 6개와 전체 `npm test && npm run verify`를 확인했다. 원격 push 전 `git fetch origin`과 `git pull --rebase origin claude/flag-game-expansion-design-rpv24x`를 실행했고 충돌은 없었다. 로컬 검증과 GitHub CI, main 배포는 서로 별도 상태다.

## 이 맥의 변환 도구

`sips`와 `/opt/homebrew/bin/cwebp` 1.6.0을 실제 변환에 사용했다. `sips`는 WebP 읽기만 지원하며, ffmpeg·ImageMagick(`magick`/`convert`)은 현재 PATH에 없다. 요청한 명령의 출력:

```text
$ sips --formats | grep -i webp
org.webmproject.webp         webp  
```
